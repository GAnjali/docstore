Docstore

Docstore is simplay an application to maintain a file system. It allows users to login, create, update, delete, share their files and folders.

command to run the app

//In project directory
docker-compose up

It will start the server at http://localhost:3000
