import React from "react";
import '../styles/Home.css';

const Header = () => {
    return (
        <nav className="navbar">
            <h2>Docstore</h2>
        </nav>
    );
};

export default Header;